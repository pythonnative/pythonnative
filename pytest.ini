[pytest]
addopts = -q
testpaths =
    tests
    src/pythonnative
asyncio_mode = auto
markers =
    network: talks to PyPI and the platform wheel indexes; gated behind PN_PACKAGE_MATRIX=1 (tests/packages)
